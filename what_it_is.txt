each payment would look like:

  value($), title, category, description

  data Payment = Payment Double String Category String


Category has a threshold associated with it
  
  data Category = Cat String Double

Basic & Initial functionality:

  - total payments so far -> DONE
  - chart up based on category -> DONE
  - chart a particular category and break it down by description
  - generate warning based on categories and their thresholds

Core functionality input/output

  - List of payments come in <- in
  - single payment come in <- in
  - Total payment info -> out
  - Chart up the category payments -> out
